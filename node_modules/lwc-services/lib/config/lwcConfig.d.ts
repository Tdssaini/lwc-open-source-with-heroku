interface ResourceFolder {
    from: string;
    to: string;
}
interface Config {
    buildDir: string;
    mode: string;
    noclear: boolean;
    moduleDir: string;
    localModulesDirs: string[];
    layout: string;
    sourceDir: string;
    resources: ResourceFolder[];
    devServer: {
        port: number;
        host: string;
        open: boolean;
        stats: string;
        noInfo: boolean;
        contentBase: string;
    };
    server: {
        port: number;
        host: string;
        open: boolean;
        customConfig?: string;
    };
    lwcCompilerOutput: {
        development?: {
            compat?: boolean;
            minify?: boolean;
            env?: {
                NODE_ENV: string;
            };
            format?: string;
        };
        production?: {
            compat?: boolean;
            minify?: boolean;
            env?: {
                NODE_ENV: string;
            };
            format?: string;
        };
    };
    lwcCompilerStylesheetConfig: any;
    lwcExperimentalDynamicComponent: any;
}
export declare const defaultLwcConfig: Config;
export declare const lwcConfig: Config;
export {};

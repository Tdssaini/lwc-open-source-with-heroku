"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const logger_1 = require("../logger");
const chalk = require('chalk');
function analyzeStats(stats) {
    logger_1.log(buildStatsMessage(`Build duration: ${chalk.green((stats.endTime - stats.startTime) / 1000)}`));
}
exports.analyzeStats = analyzeStats;
function buildStatsMessage(message) {
    const msg = {
        message: `       ${message}`
    };
    return msg;
}

import { NormalizedCompilerOptions } from '../compiler/options';
import { FileTransformerResult } from './transformer';
export default function styleTransform(src: string, filename: string, config: NormalizedCompilerOptions): FileTransformerResult;
